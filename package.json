{
  "name": "taboom-extension",
  "private": true,
  "version": "0.2.7",
  "type": "module",
  "description": "Node tooling metadata only (ES modules for node --test). Chrome ignores this file.",
  "devDependencies": {
    "happy-dom": "^20.11.2"
  }
}
