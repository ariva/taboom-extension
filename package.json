{
  "name": "taboom-extension",
  "private": true,
  "version": "0.2.10",
  "type": "module",
  "description": "Node tooling metadata only (ES modules for node --test; tsc type check). Chrome ignores this file.",
  "devDependencies": {
    "@types/chrome": "^0.2.6",
    "happy-dom": "^20.11.2",
    "typescript": "^7.0.2"
  }
}
